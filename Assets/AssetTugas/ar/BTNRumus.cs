using UnityEngine;
using UnityEngine.SceneManagement;

public class BTNRumus : MonoBehaviour
{
    // Fungsi ini akan dipanggil saat tombol "Kalkulator" di-klik
    public void GoToRumusBalok()
    {
        // Pastikan scene "kalkulator" sudah dimasukkan di Build Settings
        SceneManager.LoadScene("RumusBalok");
    }

        // Fungsi ini akan dipanggil saat tombol "Kalkulator" di-klik
    public void GoToRumusBola()
    {
        // Pastikan scene "kalkulator" sudah dimasukkan di Build Settings
        SceneManager.LoadScene("RumusBola");
    }

        // Fungsi ini akan dipanggil saat tombol "Kalkulator" di-klik
    public void GoToRumusKubus()
    {
        // Pastikan scene "kalkulator" sudah dimasukkan di Build Settings
        SceneManager.LoadScene("RumusKubus");
    }

        // Fungsi ini akan dipanggil saat tombol "Kalkulator" di-klik
    public void GoToRumusLimas()
    {
        // Pastikan scene "kalkulator" sudah dimasukkan di Build Settings
        SceneManager.LoadScene("RumusLimas");
    }

        // Fungsi ini akan dipanggil saat tombol "Kalkulator" di-klik
    public void GoToRumusTabung()
    {
        // Pastikan scene "kalkulator" sudah dimasukkan di Build Settings
        SceneManager.LoadScene("RumusTabung");
    }

        // Fungsi ini akan dipanggil saat tombol "Kalkulator" di-klik
    public void GoToRumusKerucut()
    {
        // Pastikan scene "kalkulator" sudah dimasukkan di Build Settings
        SceneManager.LoadScene("RumusKerucut");
    }
}
